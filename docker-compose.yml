# Production docker-compose configuration
services:
  web:
    build: .
    restart: always
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    extra_hosts:
      - "api-contracts.elbouazzatiholding.ma:host-gateway"
    networks:
      - webnet
    expose:
      - '3001'

  nginx:
    image: nginx:1.25-alpine
    restart: always
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - /etc/contracts_frontend/nginx/conf.d:/etc/nginx/conf.d:ro
      - /etc/contracts_frontend/nginx/certs:/etc/ssl/certs:ro
    depends_on:
      - web
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
